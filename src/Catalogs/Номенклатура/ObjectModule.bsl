Процедура ПриУстановкеНовогоКода(СтандартнаяОбработка, Префикс)
	Префикс = Константы.ПрефиксИнформационнойБазы.Получить() + "-";
КонецПроцедуры

Процедура ПередЗаписью(Отказ)
	
	НайденныйЭлемент = Справочники.Номенклатура.НайтиПоНаименованию(Наименование);
	Если НЕ НайденныйЭлемент.Пустая() И НайденныйЭлемент <> Ссылка Тогда
		Отказ = Истина;
		Сообщить("В справочнике уже есть элемент с таким наименованием, код: " + НайденныйЭлемент.Код);
	КонецЕсли; 
	
КонецПроцедуры
